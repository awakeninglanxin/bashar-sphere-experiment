# ============================================================
# 视频原件不入库
# ------------------------------------------------------------
# 本地保留 13 个 mp4 / 408.2 MB（另有 3 个由老师本人删除）。
# 其中 V18 单文件 153.6 MB，超过 GitHub 单文件 100 MB 硬上限。
# 编号 / 标题 / YouTube 地址 / 语录链接见：
#   bashar科技系列/_视频原件/00_对照清单.md
# ============================================================
*.mp4
*.mkv
*.webm
*.mov
*.part

# ============================================================
# 未脱敏原件不入库
# ------------------------------------------------------------
# 元宝分享页原始抓取内嵌预签名下载 URL（腾讯云 AKID + q-signature，
# 最长有效期至 2036-09-17）。公开版已把这 5 类参数的值换成 REDACTED。
# 字节原件留本地；其 SHA-256 溯源清单已公开：
#   _原始数据/_未脱敏原件_SHA256溯源清单.md
# ============================================================
_原始数据/_未脱敏_勿公开/

# 下载中间产物
*.ytdl
*.temp

# 操作系统
Thumbs.db
desktop.ini
.DS_Store

# ============================================================
# 版本备份不入库（仅本地留存）
# ------------------------------------------------------------
# 产出文档/_旧版备份_YYYYMMDD/ 是替换前的历史版本。公开仓库里同一内容
# 出现两份会让读者对不上号（也踩过「同一日期被重复统计」的坑），
# 因此只本地留存。指纹见各次备份目录内的说明。
# ============================================================
产出文档/_旧版备份_*/

# ============================================================
# 第三方版权书原件不入库（仅本地留存）
# ------------------------------------------------------------
# 补充对话_Harmonograph八路微小频差检测/附件/harmonograph.pdf
#   = Ashton《Harmonograph》整本扫描件，21,236,114 字节，
#     由元宝分享附件下载得到，版权属原作者与出版方。
# 本仓库只公开「读完之后落成了什么」（01_精读 / 第 7 章协议）
# 与它的 SHA-256 指纹，原件不随仓库分发。
# 说明与验证方法：补充对话_Harmonograph八路微小频差检测/_附件_不入库说明与SHA256.md
# ============================================================
补充对话_Harmonograph八路微小频差检测/附件/

# ============================================================
# 改动前的时间戳快照不入库（仅本地留存）
# ------------------------------------------------------------
# _bak/ 存放每次改动前复制的原件（如 索引页_before_annotation_20260921.html），
# 只用于本地回滚。公开仓库里放进去，读者会面对同一内容的多份副本。
# ============================================================
_bak/

# ============================================================
# 第三方版权书原件（补漏，仅本地留存）
# ------------------------------------------------------------
# 补充对话_Harmonograph八路微小频差检测/harmonograph.pdf
#   Ashton《Harmonograph》整本扫描件，21,236,114 字节，版权属原作者与出版方。
#   上面那条只覆盖了 …/附件/ 子目录，这一份直接落在会话目录下，一并排除。
# ============================================================
补充对话_Harmonograph八路微小频差检测/harmonograph.pdf
