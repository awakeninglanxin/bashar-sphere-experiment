# ============================================================
# 视频原件不入库
# ------------------------------------------------------------
# 16 个 mp4 合计 441 MB，其中 V18 单文件 153.6 MB，
# 超过 GitHub 单文件 100 MB 硬上限，直推会被拒。
# 全部视频的编号 / 标题 / YouTube 地址见：
#   bashar科技系列/_视频原件/00_对照清单.md
#   bashar科技系列/_视频原件/_下载清单.json
# 需要原件时按清单里的 URL 用 yt-dlp 重下即可。
# ============================================================
*.mp4
*.mkv
*.webm
*.mov
*.part

# ============================================================
# 未脱敏原件不入库
# ------------------------------------------------------------
# 元宝分享页原始抓取里内嵌了预签名下载 URL（腾讯云 AKID + q-signature，
# 签名有效期至 2036）。公开前已把这 5 类参数的值替换为 REDACTED，
# 替换后的正式版本已放回 _原始数据/ 原路径；
# 未脱敏的字节原件留在本目录，仅供本地留存，不得公开。
# ============================================================
_原始数据/_未脱敏_勿公开/

# 下载中间产物
*.ytdl
*.temp

# 操作系统
Thumbs.db
desktop.ini
.DS_Store
